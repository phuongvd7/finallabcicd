version: "3.9"
services:
  my-app:
    image: "phuongvd7/testmaster:latest"
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    ports:
      - "80:80"
    # volumes:
    #   - ".:/usr/share/nginx/html"